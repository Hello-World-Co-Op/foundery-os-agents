# FounderyOS Agents Configuration

# Server
PORT=3001
NODE_ENV=development

# Claude API
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ICP Configuration
IC_HOST=http://127.0.0.1:4943
FOUNDERY_OS_CORE_CANISTER_ID=
AUTH_SERVICE_CANISTER_ID=

# WebSocket
WS_PORT=3002

# CORS Origins (comma-separated list of allowed origins)
# REQUIRED in production - the service will NOT start without this
# Each origin must be a valid URL (e.g., https://app.example.com)
# Example: CORS_ORIGINS=https://app.helloworlddao.com,https://staging.helloworlddao.com
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Development Auth Bypass (FOS-5.6.1)
# Uncomment ONLY for local development without auth-service
# NEVER set this in production environments
# DEV_SKIP_AUTH=true
